[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dag_dir_view"
version = "0.0.1"

dependencies = ["apache-airflow>=2.6"]

[project.entry-points.'airflow.plugins']
dag_dir_view = "dag_dir_view:DagDirViewPlugin"
